pipeline {
agent any

stages {
   stage ('build') {
     steps {
     sh 'ant -f build.xml -v'
      echo 'building....'
           }
          }
         }
     post {
       always {
         archive 'dist/*.jar'
         echo 'archived'
           }
          }
         }
     
     
